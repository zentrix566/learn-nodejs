# learn-nodejs
learn nodejs

## 运行命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 开发模式（直接用 ts-node 运行 TypeScript） |
| `npm run build` | 编译 TypeScript 到 dist 目录 |
| `npm start` | 运行编译后的 JavaScript |

启动后在浏览器打开 http://localhost:3000 查看效果。
